# Core transcription (GPU-accelerated; uses CUDA if available, falls back to CPU)
faster-whisper

# Speaker diarization (requires HuggingFace token and model acceptance)
pyannote.audio

# HTTP client used by summarize.py to call Ollama
requests

# PyTorch — required by pyannote.audio; install the CUDA variant if you have a GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch

# HuggingFace model hub (model downloads for diarization)
huggingface-hub
